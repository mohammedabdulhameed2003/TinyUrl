<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>URL Shrinker</title>
    <style>
        body {
            background: linear-gradient(135deg, #151515, #232323);
            font-family: 'Arial', sans-serif;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #ffffff;
            margin: 0;
        }
        .container {
            max-width: 600px;
            background-color: #2c2c2c;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
        }
        h1 {
            font-size: 2.5rem;
            margin-bottom: 20px;
            color: #ffffff;
            text-align: center;
        }
        .form-inline {
            display: flex;
            justify-content: center;
        }
        .form-control {
            width: 100%;
            margin-right: 10px;
            border: 2px solid #007bff;
            border-radius: 5px;
            background-color: #3b3b3b;
            color: #ffffff;
        }
        .form-control::placeholder {
            color: #b0b0b0;
        }
        .btn {
            width: 100px;
            background-color: #007bff;
            color: #ffffff;
            border: none;
            border-radius: 5px;
        }
        .btn:hover {
            background-color: #0056b3;
        }
        .result {
            margin-top: 30px;
            text-align: center;
            background-color: #454545;
            padding: 20px;
            border-radius: 10px;
            word-break: break-all;
            color: #ffffff;
        }
        .result h3 {
            color: #28a745;
        }
        .result a {
            text-decoration: none;
            color: #007bff;
        }
        .result a:hover {
            text-decoration: underline;
        }
        .footer {
            margin-top: 20px;
            text-align: center;
            color: #b0b0b0;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>URL Shrinker</h1>
        <form action="/shortUrls" method="POST" class="my-4 form-inline">
            <input required placeholder="Enter your URL here..." type="url" name="fullUrl" id="fullUrl" class="form-control">
            <button class="btn" type="submit">Shrink</button>
        </form>

        <% if (shortUrl) { %>
        <div class="result">
            <h3>Shortened URL:</h3>
            <p>Full URL: <a href="<%= shortUrl.full %>" target="_blank"><%= shortUrl.full %></a></p>
            <p>Short URL: <a href="/<%= shortUrl.short %>" target="_blank">/<%= shortUrl.short %></a></p>
        </div>
        <% } %>
        <div class="footer">
            <p>&copy; <%= new Date().getFullYear() %> URL Shrinker</p>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+IVGVm9L1l5rXyyjX0Wl1Mu/H50Xq" crossorigin="anonymous"></script>
</body>
</html>
